import type { NetworkMode } from "@kripl/core";
import { mkdir, writeFile } from "node:fs/promises";
import { join } from "node:path";

const EXTENSION_SOURCE = "import type { ExtensionAPI } from \"@earendil-works/pi-coding-agent\";\nimport { lookup } from \"node:dns/promises\";\nimport { isIP } from \"node:net\";\nimport { Type } from \"typebox\";\n\nconst DEFAULT_MAX_BODY_BYTES = 1_000_000;\nconst DEFAULT_MAX_CHARS = 30_000;\nconst MAX_REDIRECTS = 5;\n\nfunction networkMode(): \"online\" | \"restricted\" | \"offline\" {\n  const value = process.env.KRIPL_NETWORK_MODE;\n  return value === \"offline\" || value === \"restricted\" ? value : \"online\";\n}\n\nfunction assertNetworkEnabled(): void {\n  if (networkMode() === \"offline\") {\n    throw new Error(\"Network tools are disabled by Kripl Studio offline mode.\");\n  }\n}\n\nfunction ipv4Parts(address: string): number[] | undefined {\n  const parts = address.split(\".\").map((part) => Number(part));\n  if (parts.length !== 4 || parts.some((part) => !Number.isInteger(part) || part < 0 || part > 255)) {\n    return undefined;\n  }\n  return parts;\n}\n\nfunction isBlockedIpv4(address: string): boolean {\n  const parts = ipv4Parts(address);\n  if (!parts) return true;\n  const [a = 0, b = 0] = parts;\n  return (\n    a === 0 ||\n    a === 10 ||\n    a === 127 ||\n    (a === 100 && b >= 64 && b <= 127) ||\n    (a === 169 && b === 254) ||\n    (a === 172 && b >= 16 && b <= 31) ||\n    (a === 192 && b === 0) ||\n    (a === 192 && b === 168) ||\n    (a === 198 && (b === 18 || b === 19)) ||\n    a >= 224\n  );\n}\n\nfunction isBlockedIpv6(address: string): boolean {\n  const lower = address.toLowerCase();\n  if (lower === \"::\" || lower === \"::1\") return true;\n  if (lower.startsWith(\"fc\") || lower.startsWith(\"fd\")) return true;\n  if (/^fe[89ab]/.test(lower)) return true;\n  if (lower.startsWith(\"ff\")) return true;\n  const mapped = lower.match(/^::ffff:(\\d+\\.\\d+\\.\\d+\\.\\d+)$/);\n  return mapped ? isBlockedIpv4(mapped[1] ?? \"\") : false;\n}\n\nfunction isBlockedAddress(address: string): boolean {\n  const family = isIP(address);\n  if (family === 4) return isBlockedIpv4(address);\n  if (family === 6) return isBlockedIpv6(address);\n  return true;\n}\n\nasync function assertPublicUrl(input: string): Promise<URL> {\n  let url: URL;\n  try {\n    url = new URL(input);\n  } catch {\n    throw new Error(\"Invalid URL.\");\n  }\n\n  if (url.protocol !== \"https:\" && url.protocol !== \"http:\") {\n    throw new Error(\"Only HTTP(S) URLs are supported.\");\n  }\n  if (url.username || url.password) {\n    throw new Error(\"Credential-bearing URLs are not allowed.\");\n  }\n\n  const hostname = url.hostname.replace(/^\\[|\\]$/g, \"\");\n  if (!hostname) throw new Error(\"URL hostname is required.\");\n  if (hostname.toLowerCase() === \"localhost\" || hostname.toLowerCase().endsWith(\".localhost\")) {\n    throw new Error(\"Localhost is not available to web tools.\");\n  }\n\n  if (isIP(hostname)) {\n    if (isBlockedAddress(hostname)) throw new Error(\"Private or local network addresses are blocked.\");\n    return url;\n  }\n\n  let addresses;\n  try {\n    addresses = await lookup(hostname, { all: true, verbatim: true });\n  } catch {\n    throw new Error(`Unable to resolve host: ${hostname}`);\n  }\n  if (addresses.length === 0 || addresses.some((item) => isBlockedAddress(item.address))) {\n    throw new Error(\"Host resolves to a private, local, or unsupported network address.\");\n  }\n  return url;\n}\n\nfunction createAbortSignal(parent: AbortSignal | undefined, timeoutMs: number): { signal: AbortSignal; dispose(): void } {\n  const controller = new AbortController();\n  const timer = setTimeout(() => controller.abort(new Error(\"Network request timed out.\")), timeoutMs);\n  const onAbort = () => controller.abort(parent?.reason);\n  parent?.addEventListener(\"abort\", onAbort, { once: true });\n  return {\n    signal: controller.signal,\n    dispose() {\n      clearTimeout(timer);\n      parent?.removeEventListener(\"abort\", onAbort);\n    }\n  };\n}\n\nasync function safeFetch(input: string, signal?: AbortSignal): Promise<{ response: Response; finalUrl: string }> {\n  assertNetworkEnabled();\n  let current = input;\n\n  for (let redirects = 0; redirects <= MAX_REDIRECTS; redirects += 1) {\n    const url = await assertPublicUrl(current);\n    const controlled = createAbortSignal(signal, 15_000);\n    let response: Response;\n    try {\n      response = await fetch(url, {\n        redirect: \"manual\",\n        signal: controlled.signal,\n        headers: {\n          \"user-agent\": \"Kripl-Studio/0.0.1 (+local-agent)\",\n          accept: \"text/html,application/xhtml+xml,text/plain,application/json;q=0.8,*/*;q=0.2\"\n        }\n      });\n    } finally {\n      controlled.dispose();\n    }\n\n    if ([301, 302, 303, 307, 308].includes(response.status)) {\n      const location = response.headers.get(\"location\");\n      if (!location) throw new Error(`Redirect ${response.status} did not include a location.`);\n      if (redirects === MAX_REDIRECTS) throw new Error(\"Too many redirects.\");\n      current = new URL(location, url).toString();\n      continue;\n    }\n\n    return { response, finalUrl: url.toString() };\n  }\n  throw new Error(\"Too many redirects.\");\n}\n\nasync function readBodyLimited(response: Response, maxBytes = DEFAULT_MAX_BODY_BYTES): Promise<string> {\n  if (!response.body) return \"\";\n  const reader = response.body.getReader();\n  const chunks: Uint8Array[] = [];\n  let total = 0;\n  while (true) {\n    const { done, value } = await reader.read();\n    if (done) break;\n    if (!value) continue;\n    total += value.byteLength;\n    if (total > maxBytes) {\n      await reader.cancel();\n      throw new Error(`Response exceeded ${maxBytes} bytes.`);\n    }\n    chunks.push(value);\n  }\n  const combined = new Uint8Array(total);\n  let offset = 0;\n  for (const chunk of chunks) {\n    combined.set(chunk, offset);\n    offset += chunk.byteLength;\n  }\n  return new TextDecoder().decode(combined);\n}\n\nfunction decodeHtml(value: string): string {\n  const named: Record<string, string> = {\n    amp: \"&\", lt: \"<\", gt: \">\", quot: \"\\\"\", apos: \"'\", nbsp: \" \"\n  };\n  return value.replace(/&(#x?[0-9a-f]+|[a-z]+);/gi, (_match, entity: string) => {\n    if (entity.startsWith(\"#x\") || entity.startsWith(\"#X\")) {\n      const code = Number.parseInt(entity.slice(2), 16);\n      return Number.isFinite(code) ? String.fromCodePoint(code) : \"\";\n    }\n    if (entity.startsWith(\"#\")) {\n      const code = Number.parseInt(entity.slice(1), 10);\n      return Number.isFinite(code) ? String.fromCodePoint(code) : \"\";\n    }\n    return named[entity.toLowerCase()] ?? `&${entity};`;\n  });\n}\n\nfunction htmlToText(html: string): string {\n  return decodeHtml(\n    html\n      .replace(/<!--[^]*?-->/g, \" \")\n      .replace(/<(script|style|noscript|svg|canvas|template)\\b[^>]*>[^]*?<\\/\\1>/gi, \" \")\n      .replace(/<(br|hr)\\b[^>]*>/gi, \"\\n\")\n      .replace(/<\\/(p|div|section|article|main|header|footer|nav|li|h[1-6]|tr)>/gi, \"\\n\")\n      .replace(/<[^>]+>/g, \" \")\n  )\n    .replace(/[ \\t\\f\\v]+/g, \" \")\n    .replace(/ *\\n */g, \"\\n\")\n    .replace(/\\n{3,}/g, \"\\n\\n\")\n    .trim();\n}\n\nfunction extractTitle(html: string): string | undefined {\n  const match = html.match(/<title\\b[^>]*>([^]*?)<\\/title>/i);\n  if (!match?.[1]) return undefined;\n  const title = htmlToText(match[1]);\n  return title || undefined;\n}\n\nfunction unwrapDuckDuckGoUrl(raw: string): string {\n  const decoded = decodeHtml(raw);\n  try {\n    const url = new URL(decoded, \"https://html.duckduckgo.com\");\n    const target = url.searchParams.get(\"uddg\");\n    return target ? decodeURIComponent(target) : url.toString();\n  } catch {\n    return decoded;\n  }\n}\n\ninterface SearchResult { title: string; url: string; snippet: string }\n\nfunction parseDuckDuckGo(html: string, limit: number): SearchResult[] {\n  const results: SearchResult[] = [];\n  const anchor = /<a[^>]*class=\"[^\"]*result__a[^\"]*\"[^>]*href=\"([^\"]+)\"[^>]*>([^]*?)<\\/a>/gi;\n  let match: RegExpExecArray | null;\n  while ((match = anchor.exec(html)) && results.length < limit) {\n    const rawUrl = match[1];\n    const rawTitle = match[2];\n    if (!rawUrl || !rawTitle) continue;\n    const after = html.slice(anchor.lastIndex, anchor.lastIndex + 3500);\n    const snippetMatch = after.match(/<(?:a|div)[^>]*class=\"[^\"]*result__snippet[^\"]*\"[^>]*>([^]*?)<\\/(?:a|div)>/i);\n    const url = unwrapDuckDuckGoUrl(rawUrl);\n    if (!/^https?:\\/\\//i.test(url)) continue;\n    results.push({\n      title: htmlToText(rawTitle),\n      url,\n      snippet: snippetMatch?.[1] ? htmlToText(snippetMatch[1]) : \"\"\n    });\n  }\n  return results.filter((item) => item.title);\n}\n\nfunction parseBing(html: string, limit: number): SearchResult[] {\n  const results: SearchResult[] = [];\n  const block = /<li[^>]*class=\"[^\"]*b_algo[^\"]*\"[^>]*>([^]*?)<\\/li>/gi;\n  let match: RegExpExecArray | null;\n  while ((match = block.exec(html)) && results.length < limit) {\n    const body = match[1] ?? \"\";\n    const anchor = body.match(/<h2[^>]*>\\s*<a[^>]*href=\"([^\"]+)\"[^>]*>([^]*?)<\\/a>/i);\n    if (!anchor?.[1] || !anchor[2]) continue;\n    const snippet = body.match(/<p[^>]*>([^]*?)<\\/p>/i);\n    if (!/^https?:\\/\\//i.test(anchor[1])) continue;\n    results.push({\n      title: htmlToText(anchor[2]),\n      url: decodeHtml(anchor[1]),\n      snippet: snippet?.[1] ? htmlToText(snippet[1]) : \"\"\n    });\n  }\n  return results.filter((item) => item.title);\n}\n\nasync function searchWeb(query: string, limit: number, signal?: AbortSignal) {\n  const ddgUrl = `https://html.duckduckgo.com/html/?q=${encodeURIComponent(query)}`;\n  try {\n    const { response } = await safeFetch(ddgUrl, signal);\n    if (response.ok) {\n      const html = await readBodyLimited(response, 1_500_000);\n      const results = parseDuckDuckGo(html, limit);\n      if (results.length > 0) return { engine: \"duckduckgo\", results };\n    }\n  } catch {}\n\n  const bingUrl = `https://www.bing.com/search?q=${encodeURIComponent(query)}&count=${limit}`;\n  const { response } = await safeFetch(bingUrl, signal);\n  if (!response.ok) throw new Error(`Search failed with HTTP ${response.status}.`);\n  const html = await readBodyLimited(response, 1_500_000);\n  const results = parseBing(html, limit);\n  if (results.length === 0) throw new Error(\"Search provider returned no parseable results.\");\n  return { engine: \"bing\", results };\n}\n\nexport default function (pi: ExtensionAPI) {\n  pi.registerTool({\n    name: \"web_search\",\n    label: \"Web Search\",\n    description: \"Search the public web. Use this for current documentation, releases, news, and other information not available in the workspace.\",\n    promptSnippet: \"Search the public web for current information\",\n    promptGuidelines: [\n      \"Use web_search when the task depends on current or external information.\",\n      \"Open relevant results with web_open before relying on detailed claims.\"\n    ],\n    parameters: Type.Object({\n      query: Type.String({ minLength: 1, maxLength: 1000, description: \"Search query\" }),\n      maxResults: Type.Optional(Type.Integer({ minimum: 1, maximum: 10, description: \"Maximum number of results\" }))\n    }),\n    async execute(_toolCallId, params, signal) {\n      const query = params.query.trim();\n      if (!query) throw new Error(\"Search query must not be empty.\");\n      const maxResults = params.maxResults ?? 6;\n      const result = await searchWeb(query, maxResults, signal);\n      const text = result.results\n        .map((item, index) => `${index + 1}. ${item.title}\\n${item.url}${item.snippet ? `\\n${item.snippet}` : \"\"}`)\n        .join(\"\\n\\n\");\n      return {\n        content: [{ type: \"text\", text: `Search engine: ${result.engine}\\n\\n${text}` }],\n        details: result\n      };\n    }\n  });\n\n  pi.registerTool({\n    name: \"web_open\",\n    label: \"Web Open\",\n    description: \"Open an HTTP(S) page and extract readable text. Local/private network targets and credential-bearing URLs are blocked.\",\n    promptSnippet: \"Open a public web page and read its contents\",\n    promptGuidelines: [\n      \"Use web_open on search results or direct documentation URLs.\",\n      \"Prefer the canonical source when several pages describe the same fact.\"\n    ],\n    parameters: Type.Object({\n      url: Type.String({ minLength: 1, maxLength: 4096, description: \"Public HTTP(S) URL\" }),\n      maxChars: Type.Optional(Type.Integer({ minimum: 1000, maximum: 50000, description: \"Maximum extracted text characters\" }))\n    }),\n    async execute(_toolCallId, params, signal) {\n      const { response, finalUrl } = await safeFetch(params.url, signal);\n      if (!response.ok) throw new Error(`HTTP ${response.status} ${response.statusText}`.trim());\n      const contentType = (response.headers.get(\"content-type\") ?? \"\").toLowerCase();\n      if (!contentType.includes(\"text/\") && !contentType.includes(\"json\") && !contentType.includes(\"xml\")) {\n        throw new Error(`Unsupported content type: ${contentType || \"unknown\"}`);\n      }\n      const raw = await readBodyLimited(response);\n      const isHtml = contentType.includes(\"html\") || /<html\\b|<!doctype\\s+html/i.test(raw.slice(0, 2048));\n      const title = isHtml ? extractTitle(raw) : undefined;\n      const text = isHtml ? htmlToText(raw) : raw.trim();\n      const maxChars = params.maxChars ?? DEFAULT_MAX_CHARS;\n      const truncated = text.length > maxChars;\n      const output = text.slice(0, maxChars);\n      return {\n        content: [{\n          type: \"text\",\n          text: [\n            `URL: ${finalUrl}`,\n            title ? `Title: ${title}` : \"\",\n            `Content-Type: ${contentType || \"unknown\"}`,\n            truncated ? `[truncated to ${maxChars} characters]` : \"\",\n            \"\",\n            output\n          ].filter(Boolean).join(\"\\n\")\n        }],\n        details: { url: finalUrl, title, contentType, truncated, characters: output.length }\n      };\n    }\n  });\n}\n";

export async function writePiWebTools(agentDir: string, networkMode: NetworkMode): Promise<void> {
  const extensionsDir = join(agentDir, "extensions");
  await mkdir(extensionsDir, { recursive: true });
  await writeFile(join(extensionsDir, "kripl-web-tools.ts"), EXTENSION_SOURCE, {
    encoding: "utf8",
    mode: 0o600
  });
}
